services:
  - type: web
    name: meteoro-x-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn meteoro_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
    healthCheckPath: /api/health
